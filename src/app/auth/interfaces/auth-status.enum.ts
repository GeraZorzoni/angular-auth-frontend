

export enum AuthStatus {
  // Forma de enumerar para que el sistema no asigne directamente 0,1,2 evitando errores

  checking         = 'checking',
  authenticated    = 'authenticated,',
  notAuthenticated = 'notAuthenticated',

}
